<h1>id: <%= @message.id %> のメッセージ編集ページ</h1>

<%= form_with(model: @message, local: true) do |f| %>
  <%= f.label :コンテンツ, 'メッセージ' %>
  <%= f.text_field :コンテンツ %>
  <%= f.submit '投稿' %>
<% end %>
<%= link_to '一覧に戻る', messages_path %>